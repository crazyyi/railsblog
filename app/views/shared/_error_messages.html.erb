<% if messages.errors.any? %>
		<div id="errors">
			<h2><%= pluralize(messages.errors.count, "error") %> encountered, please check your input.</h2>

			<ul>
				<% messages.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
<% end %>